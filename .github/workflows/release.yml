# ═══════════════════════════════════════════
# Release is done LOCALLY via release.bat
# Binaries are pushed to jinkaka98.github.io
# This workflow is intentionally disabled.
# ═══════════════════════════════════════════
#
# To release:
#   1. Edit VERSION file
#   2. Run release.bat
#   3. Binaries auto-pushed to public repo
#
name: Release (disabled)
on: workflow_dispatch
jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Release is done locally via release.bat"
